
class Book{
  constructor(id, title, author, pages, isRead){
    this.id = id,
    this.title = title,
    this.author = author,
    this.pages = pages,
    this.isRead = isRead
  }
}

export default Book